import 'package:flutter/material.dart';

class AccontPage extends StatelessWidget {
  const AccontPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  Center(child:  Text("Accont"),),
    );
  }
}